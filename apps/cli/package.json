{
  "name": "@meawhooks/cli",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "meawhooks": "./src/index.tsx"
  },
  "scripts": {
    "dev": "bun --env-file=.env run src/index.tsx listen --tunnel test http://localhost:9999/hook",
    "listen": "bun --env-file=.env run src/index.tsx listen",
    "build": "bun build src/index.tsx --outdir dist --target bun --external react-devtools-core",
    "lint": "tsc --noEmit",
    "test": "bun test --isolate src/lib && bun test src/commands/listen.test.tsx"
  },
  "dependencies": {
    "ink": "^7.0.6",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "ink-testing-library": "^4.0.0",
    "typescript": "^5.8.3"
  }
}
